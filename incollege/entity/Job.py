class Job:

    def __init__(self, job_id, owner_user_id, title, desc, employer, location, salary):
        self.job_id = job_id
        self.owner_user_id = owner_user_id
        self.title = title
        self.desc = desc
        self.employer = employer
        self.location = location
        self.salary = salary
